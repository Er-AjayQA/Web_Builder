{
  "name": "builder_be",
  "version": "1.0.0",
  "description": "This is web builder backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:create": "sequelize-cli db:create",
    "migrate": "sequelize-cli db:migrate",
    "seed": "sequelize-cli db:seed:all",
    "start": "(npm run db:create || echo \"DB already exists\") && npm run migrate && npm run seed && nodemon server.js"
  },
  "author": "Er.Ajay Kumar",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "moment": "^2.30.1",
    "multer": "^2.1.1",
    "mysql": "^2.18.1",
    "mysql2": "^3.22.1",
    "nodemailer": "^8.0.5",
    "sequelize": "^6.37.8",
    "slugify": "^1.6.9",
    "typescript": "^6.0.3",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.5"
  }
}
